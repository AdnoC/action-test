name: Create release candidate PR

on:
  workflow_dispatch
  pull_request

jobs:
  create_release:
    name: Create release
    runs-on: ubuntu-latest
    steps:
      - run: echo 'doing release thing'
